{
  "name": "web-omni-lan-runtime",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "build:crypto": "esbuild secure-channel-source.js --bundle --format=iife --global-name=WebOmniLanSecure --target=chrome114,safari15 --outfile=secure-channel.js --minify",
    "check": "node --check transfer.js && node --check mobile-runtime.template.js && node --check secure-channel.js",
    "test": "node secure-channel.test.mjs"
  },
  "dependencies": {
    "@noble/ciphers": "2.2.0",
    "@noble/curves": "2.2.0",
    "@noble/hashes": "2.2.0"
  },
  "devDependencies": {
    "esbuild": "0.28.1"
  }
}
